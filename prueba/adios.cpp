class HolaMundo
{
public:
    HolaMundo();
    HolaMundo(HolaMundo &&) = default;
    HolaMundo(const HolaMundo &) = default;
    HolaMundo &operator=(HolaMundo &&) = default;
    HolaMundo &operator=(const HolaMundo &) = default;
    ~HolaMundo();

private:
    
};

HolaMundo::HolaMundo()
{
    for (size_t i = 1; i < +1; i++)
    {
        dñjfak

    }

}

while ()
{
    
}

HolaMundo::~HolaMundo()
{
}